{
  "name": "@purpusgit/service-auth-client",
  "version": "0.1.0",
  "description": "One shared implementation of service_auth token introspection for every Node service in the estate.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {},
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/node": "^20.12.7",
    "@types/supertest": "^6.0.2",
    "express": "^4.18.2",
    "supertest": "^7.0.0",
    "typescript": "^5.4.5",
    "vitest": "^2.1.9"
  },
  "peerDependencies": {
    "express": "^4.18.2"
  }
}